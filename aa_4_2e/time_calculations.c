#include "time_calculations.h"

clock_t calculate_time(clock_t a[4], function_resourse_t *fr, int count)
{
	clock_t min = fr->begin;
	clock_t max = fr->end;
	for (int i = 1; i < count; i++)
	{
		if ()
	}
}
